/**
 * سياسة الوصول عند تفعيل التحقق من الصلاحيات على المسارات:
 *
 * - إن لم يكن لدى المستخدم الصلاحية المطلوبة للمسار الحالي، يُعرض **محتوى صفحة
 *   «ليس لديك صلاحية» فقط** (بدون إعادة توجيه خفيّة إلى الرئيسية وبدون استثناء
 *   لمجموعات أو أدوار معيّنة بجانب قواعد الصلاحيات نفسها).
 *
 * استثناء تقني وحيد: مسار عرض رسالة الرفض نفسه يجب أن يبقى متاحًا لأي مستخدم
 * مسجّل الدخول حتى لا يحدث حلقة توجيه.
 */
export const STRICT_PATH_ACCESS_DENIED_PAGE = true;

/** مسار صفحة الرفض (نسبي إلى جذر التطبيق المحمي `/`) */
export const ACCESS_DENIED_PATH = "access-denied" as const;

/**
 * عند `true`: يُتحقق من `routePermissionRules` ويُعاد التوجيه إلى `access-denied`
 * إن لم تتوفر أي من الصلاحيات المطلوبة.
 * (يُفترض تنفيذ هجرات `20260501130000` و`20260501130100` على Supabase.)
 */
export const ENABLE_PATH_PERMISSION_CHECK = true;
